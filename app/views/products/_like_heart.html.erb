<%if @guest.variants.include?(variant)%>
    <%=link_to '', dislike_variant_path(variant), class: 'item__like-off active', method: :delete, remote: true%>
<%else%>
    <%=link_to '', like_variant_path(variant), class: 'item__like-off', method: :post, remote: true%>
<%end%>